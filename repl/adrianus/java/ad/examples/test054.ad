class Flag {
    colors = ["red", "white", "purple"]
}

class Country {
    flag = Flag()

    def constructor(name) {
        this.name = name
    }

    def getFlag(n) {
        println(2 * n)
        return flag;
    }
}

class Continent {
    country = Country("USA")
}

america = Continent()
println(america.country.getFlag(4).colors)
