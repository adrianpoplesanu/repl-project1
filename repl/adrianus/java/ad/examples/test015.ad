class Server {
    port = 8080

    def add_security_offset(offset) {
        port = port + offset
    }

    def listen() {
        println("listening")
        println(port)
    }
}

server = Server()
server.add_security_offset(1)
server.listen()
