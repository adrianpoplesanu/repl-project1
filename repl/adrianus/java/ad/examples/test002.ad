println("aaa")

let fib = func(x) {
    if (x == 0) {
        return 0;
    }
    if (x == 1) {
        return 1;
    }
    return fib(x - 1) + fib(x - 2);
}

println("bbb")

let b = fib(8)
println(b)
