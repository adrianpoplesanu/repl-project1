class Flag {
    colors = ["red", "white", "blue"]
}

class Country {

    def constructor(name) {
        this.name = name
        this.flag = Flag()
    }
}

country = Country("USA")
println(country.name)
println(country.flag.colors)
