class Person {
    name = "default name"

    fun constructor(name) {
        this.name = name
    }

    fun greet() {
        print(this.name)
    }
}

person = Person() // constructorul are un parametru, name
person.greet()
person.name = "custom name"
person.greet()
