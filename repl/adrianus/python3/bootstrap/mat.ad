class __AdMatrixLibrary {
    def generateMatrix(n, m, obj) {
        result = list(n, list(m, obj))
        return result
    }

    def printMatrix(matrix) {
       n = len(matrix)
       if (n > 0) {
           m = len(matrix[0])
           for (i = 0; i < n; i++) {
               print (matrix[i])
           }
       } else {
           print ("")
       }
    }
}

AdMatrixLibrary = __AdMatrixLibrary()
