class Server {
    def init(p, t) {
        port = p
        timeout = t
    }

    def listen() {
        println("listening")
        println(port)
        println("timeout")
        println(timeout)
    }
}

server = Server()
server.init(8080, 90)
server.listen()
