class Server {
    security_offset = 2

    def init() {
        port = 8080
    }

    def print_port() {
        println(__locals())
        println(port + security_offset)
    }
}

server = Server()
server.init()
server.print_port()
