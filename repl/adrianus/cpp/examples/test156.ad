/* testing callback */

def thread_work(callback, arguments) {
    callback(arguments)
}

def analyze(arguments) {
    println(len(arguments))
    println(arguments)
}

thread_work(analyze, [1, "aaa", true])
